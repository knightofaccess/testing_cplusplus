#include<iostream>
using namespace std;
int main ()
{
    char val;
    cout<<"Enter a character:" ;
    cin>>val;
    cout<<"Character entered is:"<<val<<"\n";
    system ("pause");
    return 0;
}
//char will only allow one letter to be stored so if you put a string only the first letter will be shown.
